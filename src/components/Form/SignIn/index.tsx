"use client";

import React, { Dispatch, SetStateAction } from "react";

interface Props {
  setIsActiveModal: Dispatch<SetStateAction<boolean>>;
}

const SignIn = ({ setIsActiveModal }: Props) => {
  return <div></div>;
};

export default SignIn;
